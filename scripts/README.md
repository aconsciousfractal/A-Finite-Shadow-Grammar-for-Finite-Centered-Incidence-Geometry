# Scripts

This interface package does not include standalone replay scripts.  The numerical rows discussed in the paper are source-locked in cited public companion repositories or bounded companion artifacts summarized in `artifacts/source_instance_manifest.json`.

The local reproducibility target for this repository is the LaTeX paper build and the SHA-256 package manifest.